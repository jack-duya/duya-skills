interface:
  display_name: "渡鸦 · 更新渡鸦"
  short_description: "检查并更新已安装的渡鸦duya-skills，获取官方GitHub最新版本，备份被替换文件并保留项目记忆。"
  default_prompt: "用 $duya-update，更新渡鸦，保留我的项目记忆，完成后告诉我版本和需要怎样重新加载。"
policy:
  allow_implicit_invocation: true
